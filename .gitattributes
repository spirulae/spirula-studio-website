# Vendored libraries are minified; a diff of them is never worth reading.
assets/js/vendor/* -diff linguist-vendored

# Binary media — no textual diff, and never line-ending mangled.
*.png binary
*.jpg binary
*.mp4 binary
