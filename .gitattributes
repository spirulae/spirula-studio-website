# The bundle is a single ~30 MB machine-generated line-soup. Diffing or merging
# it is never useful and is slow enough to hang a terminal.
index.html          -diff -merge
spirula-studio.html -diff -merge
